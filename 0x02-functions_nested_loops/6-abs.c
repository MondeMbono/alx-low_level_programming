#include "main.h"
/**
 * _abs - prints the absolute value
 * @z: will return absolut value
 * Return: returns absolute value
 * By Monde
 */
int _abs(int z)
{


	if (z < 0)
	{
		return (-z);
	}
	else
	{
		return (z);
	}

}
